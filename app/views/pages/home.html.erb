<div class="grid">
    <% @posts.shuffle.each do |post|%>
    <div class="item" style="background-image: url('<%= cl_image_path post.photos.key,  :width=>"auto:100:800", crop: :scale %>')">
        <div class="card-noirfuture" >
            <div class='description'>
                <%= post.description.upcase %>
            </div>
        </div>
    </div>
    <% end %>
</div>
